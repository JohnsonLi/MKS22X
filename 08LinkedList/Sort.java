public class Sort{

    @SuppressWarnings("unchecked") MyLinkedListImproved[] chumBucket = new MyLinkedListImproved[10];
    for(int i = 0; i < 10; i++){
        chumBucket[i] = new MyLinkedListImproved<Integer>(); 
        System.out.println(chumBucket.toString());
    }
}